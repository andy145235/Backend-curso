spring:
  application:
    name: ms-res
  cloud:
    config:
      uri: http://config-server:8888
      fail-fast: true
      retry:
        initial-interval: 3000  # 3 segundos
        max-interval: 10000     # 10 segundos
        max-attempts: 20
