spring:
  application:
    name: m-cursos
  cloud:
    config:
      uri: http://config-server:8888
      fail-fast: true
      retry:
        initial-interval: 3000  # 3 segundos
        max-interval: 10000     # <--- AGREGAR ESTO (10 segundos)
        max-attempts: 20